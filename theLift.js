const queues = [
    [], // G
    [3], // 1
    [4], // 2
    [], // 3
    [5], // 4
    [], // 5
    [], // 6
];

const theLift = function(queues, capacity) {
    let lift = 'LIFT';
    let mutatedQs = queues;

    mutatedQs[0].unshift(lift)

    console.log(mutatedQs);
    // Your code here!
    function liftSimulator() {

    }

    function goUpOrDown() {
        // mutatedQs

    }

    return [999]
}

